export const enum PermissionLevels {
	Everyone = 0,
	Moderator = 5,
	Admin = 6,
	ServerOwner = 7,
	BotOwner = 10
}

export enum Band {
	LOW = 'low',
	MEDIUM = 'medium',
	HIGH = 'high',
	DEFAULT = 'default'
}
